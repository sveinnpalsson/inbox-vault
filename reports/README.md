# reports/

This folder is for **local runtime/debug artifacts** and is ignored by default.

Commit policy:
- Do **not** commit account-derived JSON/text dumps (profile rebuild snapshots, prompt payloads, DB extracts).
- If documentation needs a report artifact, create a sanitized top-level markdown summary with synthetic/example addresses only.
- Keep raw per-run outputs under ignored subdirectories.
